FROM node:22-alpine  AS build

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm install --legacy-peer-deps --only=production
RUN npm install lightningcss@latest --legacy-peer-deps

COPY . .

RUN npm run build

EXPOSE 3000

CMD ["node", ".next/standalone/server.js"]
